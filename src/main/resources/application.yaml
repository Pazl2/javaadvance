spring:
  application:
    name: javaadvance

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/javaadvance}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:3049}

  hibernate:
    ddl-auto: none

  liquibase:
    enabled: true



